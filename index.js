/*function ask(msg) {
	process.stdout.write(msg + '(y/n/q)?');
	process.stdin.on('data', (data) => {
		r = data.toString().trim();
		if (r === 'y' || r === 'n' || r === 'q') {
			process.stdin.end();
			return r;
		}
	});
}

exports.ask = ask;*/

class ObjectNoid {
}
